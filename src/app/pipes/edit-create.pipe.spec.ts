import { EditCreatePipe } from './edit-create.pipe';

describe('EditCreatePipe', () => {
  it('create an instance', () => {
    const pipe = new EditCreatePipe();
    expect(pipe).toBeTruthy();
  });
});
